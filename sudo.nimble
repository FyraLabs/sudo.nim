# Package

version       = "0.1.0"
author        = "madonuko"
description   = "Detect if you are running as root, restart self with sudo if needed or setup uid zero when running with the SUID flag set."
license       = "MIT"
srcDir        = "src"


# Dependencies

requires "nim >= 1.6.12"
requires "sweet >= 0.1.1"
requires "results"
